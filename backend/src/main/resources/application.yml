spring:
  application:
    name: dexora

  datasource:
    url: jdbc:postgresql://localhost:5432/dexora_db
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: none

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8081